#include <stdio.h>

int main() {
    int X;

    // Read the input number
    scanf("%d", &X);

    // Check if the number is even or odd
    if (X % 2 == 0) {
        printf("Even
");
    } else {
        printf("Odd
");
    }

    return 0;
}

